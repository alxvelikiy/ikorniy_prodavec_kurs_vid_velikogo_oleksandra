# STATE — журнал фаз

Гілка `pipeline`. Комміт після кожного кроку. До Фази 4 не редагується `v2/text/lessons/*`, `v2/site*`, `v2/build/*`. `git push` — тільки після явного підтвердження власника (поки що не було, не пушив).

## Ф0. Інвентаризація — ЗАВЕРШЕНО

**Зроблено:**
- `v2/corpus/manifest.json` — 140 записів: 26 еталонних (E01–E26), 73 класифіковані дзвінки (C001–C073), 11 дублів (C074–C084), 1 виключений (C085), 15 зустрічей-навчань (M01–M15, з варіантами ОБ-UA/ОБ-RU), 7 «Новий запис» (N34–N40), 7 архівів (A01–A07). Кожен запис: id, тип, шлях, розмір, sha256. Скрипт побудови — `v2/corpus/_build_manifest.mjs` (детермінований: id береться з наявного `SOURCES_REGISTER.md`, дані по звонках — з `classification.json`, хеші рахуються напряму з файлів).
- `v2/corpus/RECONCILE.md` — звід 26/71/73/99 в одну таблицю з поясненням розбіжностей (71 ≠ 73 через 2 крайні outcome; 11 дублів, а не 10 — знайдена й підтверджена змістовно пропущена пара в `dedup_report.json` чату B).
- `v2/corpus/_from_chat_B/` — скопійовано `final_pool.json`, `classified_new_calls.json`, `unique81_list.json`, `dedup_report.json`, `wf_6fc9acd5-7b0.json` з тимчасових шляхів (`AppData/Local/Temp/claude/`, `.claude/projects/.../workflows/`) — ці файли поза репозиторієм могли зникнути, тепер збережені.
- Перевірено змістовно (Read обох файлів кожної пари): UA06–UA09 дійсно дублюють RU01–RU04 (та сама розмова, різні мови транскрипції).
- Перевірено: «Новий запис 34–40» дійсно існують на диску (7 файлів), ім'я пошкоджене (mojibake) через кодування файлової системи.
- sha256-перевірка на колізії по всіх 140 файлах — непозначених дублів, крім відомих 11 пар, не знайдено.

**Gate:** числа зведені без протиріч — так, з поясненими розбіжностями (71/73, 10/11 дублів). Записано в RECONCILE.md.

**Допущення (дефолт, не питав власника):**
1. Для 2 крайніх outcome («повторний контакт» C051, «частина дзвінка» C058) — не рахувати як окремий дзвінок при обчисленні часток у Ф2, але залишити в пулі 99 файлів; де це змінює базу — вказувати явно в кожному факті.
2. `dedup_report.json` чату B не перезаписувався (залишений в `_from_chat_B/` для історії); коректний список 11 дублів узятий з `classification.json.dup` і зафіксований у `manifest.json`.
3. ID-схема (E/C/M/N/A) взята без змін з `v2/handoff/SOURCES_REGISTER.md` (вже написаного іншим чатом) замість вигадування нової — уникнення розсинхрону між двома реєстрами.
4. Для M01–M15 у `manifest.json` не призначено `outcome_group` — зустрічі не мають результату «успіх/відмова», це джерело контенту (правило Ф1).

**Не заблоковано нічого** — приватність, публікація й затвердження чисел в уроки в Ф0 не зачіпались.

**Далі:** Ф1 (зустрічі → картки), починаючи з 7 «Новий запис 34–40» + ОБ-RU05, ОБ-RU08.

## Зауваження власника (2026-09-24, після Ф0) — виправлено

1. Не робив git push / PR — підтверджено, не змінилось.
2. `classification.json` і `v2/audit/*` не змінювались (`git diff HEAD` порожній, перевірено). `RECONCILE.md` доповнено: 2 межових дзвінки (C051 «повторний контакт», C058 «частина дзвінка») тепер окремою таблицею, явно НЕ додані в жодну з груп 46/6/19.
3. `manifest.json` мав лише 7 архівів (A01–A07) замість 16 з `SOURCES_REGISTER.md` — це був недогляд у першій версії скрипта, не свідоме рішення. Виправлено: додано A08–A16, тепер 149 записів.
5. `v2/corpus/_from_chat_B/*` (містить номери телефонів клієнтів у шляхах) прибрано з git-трекінгу через `.gitignore` — репозиторій не підтверджений як приватний. Файли лишились на диску. Історія git НЕ переписувалась (попередній коміт `b6b54a4` все ще містить ці файли локально — рушити старий коміт не буду без прямої вказівки, і поки нічого не запушено, ризику для зовнішніх немає).

Пункт 4 (Ф1 хвилями по 4, зупинка на 429) — продовжується нижче.

## Ф1. Зустрічі → картки — В ПРОЦЕСІ

Шардинг: 82 шарди (~15КБ, межі абзаців), детерміновано скриптом `_build_shards.mjs`. Скрипт перевірки цитат — `verify_cards.mjs` (дослівність по шарду + матриця покриття).

Хвиля 1 (N34_c01–03, N35_c01): 4/4 успішно, 57 верифікованих карток, 0 відкинутих, 1 порожній шард (N34_c03).
Хвиля 2 (N35_c02–05): 4/4 успішно, +79 карток (136 сумарно верифіковано), 0 відкинутих.

Правило 429: якщо агент повертає помилку ліміту — зупинити хвилю, записати сюди які саме шарди впали, повторити тільки їх (не всю хвилю).

## Зауваження власника (2026-09-24, після хвилі 8) — виправлено

1. **Пояснення "21/82" замість "24":** це була моя помилка формулювання в коміт-месседжі хвилі 6, не помилка даних. Скрипт `verify_cards.mjs` завжди рахував правильно й окремо: `covered` (шарди з картками) і `empty` (шарди без матеріалу, підтверджено). У коміті хвилі 6 я написав "21/82 шарди покрито", узявши лише цифру `covered` (21) зі звіту скрипта ("shards covered: 21 | empty: 3"), забувши додати `empty` (3) — тобто фактично оброблено було 21+3=24 з 82. Дані (`coverage_matrix.json`, `cards_verified.json`) увесь час були коректні; неточним було тільки моє словесне зведення. **Надалі звітую "оброблено (covered+empty)/82"**, а не лише covered. Повна таблиця всіх 82 шардів — `v2/corpus/SHARD_TABLE.md` (оновлюється після кожної хвилі).
2. **Правило RU-першоджерело для UA/RU-пар зустрічей** — рішення власника, НЕ факт з `brief.md` (перевірено: `brief.md` рядки 72-73, 80 називають їх просто "русская/украинская транскрипция" й документує дублі UA06-09=RU01-04, але не каже, яка мова первинна). Застосовано в `_apply_relevance_dedup.mjs`: кожна картка з парного джерела (M01-M13, які мають і UA, і RU версію — M14/M15 тільки UA, RU-версії нема) отримує `pair_id` (M01..M13), `primary_lang: "ru"`, `is_primary_source` (true для ОБ-RU, false для ОБ-UA). При розбіжності сенсу між UA і RU версією одного факту в синтезі (Ф3) — пріоритет RU.

## Перенесення в хмарну сесію (2026-09-24)

Гілку `pipeline` запушено на GitHub і продовжено у паралельній хмарній сесії (та сама назва чату, окремий Claude Code екземпляр) — локальна сесія впала в ліміт на хвилі 18. ROOT-константа в усіх скриптах `v2/corpus/*.mjs` була хардкоджена на `C:/Users/User/Desktop/...` — замінена на шлях, обчислений відносно розташування скрипта (портативно, детерміновано, перевірено ідентичним результатом до і після). Далі хвилі йдуть з хмарної сесії, теж по 4 шарди, з тим самим контрактом/дедупом/верифікацією після кожної хвилі.

Хвиля 18 (M12-RU_c03, M13-UA_c01, M13-RU_c01, M13-RU_c02): 4/4 успішно, +95 карток (1281 верифіковано), 72/82 оброблено (67 covered + 5 empty).
Хвиля 19 (M13-RU_c03, M13-RU_c04, M13-RU_c05, M14-UA_c01): 4/4 успішно, +126 карток (1407 верифіковано), 76/82 оброблено (71 covered + 5 empty). M13 завершено повністю (UA+RU).
Хвиля 20 (M14-UA_c02, M14-UA_c03, M14-UA_c04, M14-UA_c05): 4/4 успішно, +7 карток (1414 верифіковано), 80/82 оброблено (72 covered + 8 empty). 3 з 4 шардів порожні — M14 (внутрішня нарада «Записи обучения вживую 14») переважно про CRM-операції відділу, а не навчання новачків продажу. `EMPTY_REASONS.md` перегенеровано повністю з поточних `empty_reason` (був застарілим із хвилі 6, тепер синхронізовано з усіма 8).
Хвиля 21 (M14-UA_c06, M15-UA_c01): 2/2 успішно, +16 карток (1430 верифіковано), **82/82 оброблено (73 covered + 9 empty) — Ф1 ЗАВЕРШЕНО**.

## Ф1 — ГЕЙТ (82/82), звіт власнику

**Зроблено:**
- Усі 82 шарди (15 джерел: N34-N40, M01-M15 з UA/RU-парами де є) прочитано агентами, картки витягнуто дослівно.
- 1430 верифікованих карток (`cards_verified.json`) — кожен `text_verbatim` підтверджено скриптом `verify_cards.mjs` як буквальний (з нормалізацією пробілів) підрядок відповідного файлу-шарду; 5 карток відкинуто як недослівні (`cards_unverified.json`).
- 9 з 82 шардів — підтверджено порожні (не помилка агента, кожен з задокументованою причиною в `cards/EMPTY_REASONS.md`): переважно внутрішні CRM/операційні наради відділу або технічні паузи, без матеріалу про продажі.
- **Контракт-перевірка (п.1 завдання власника):** `_check_contract.mjs` перезапускається після кожної хвилі на всіх картках. **244 з 1435 карток (17%) не проходять контракт** — позначені `contract_valid:false`, НЕ видалені. Розбивка по полю: `speaker` — 8 карток, `lesson_hint` — 242 картки (одна картка може не проходити по обох полях). Це успадкована ситуація з хвиль 1-17 (до мого підключення) — жоден новий файл хвиль 18-21 у цей список не потрапив (перевірено grep після кожної хвилі).
- **context_before/context_after (п.2):** додається `_apply_relevance_dedup.mjs` для кожної картки — 1-2 сусідні абзаци шарду навколо цитати; якщо цитата зустрічається в шарді неоднозначно (>1 збіг), поле `ambiguous_location` це фіксує.
- **Дедуп Ф1.5 (п.3):** RU/UA версії однієї зустрічі НІКОЛИ не позначаються дублями за змістом (`pair_id`/`primary_lang`/`is_primary_source` лише інформують про пару, RU — першоджерело за рішенням власника). Дублем позначається лише точний нормалізований збіг тексту або near-duplicate (Jaccard ≥0.9) **в межах однієї мови джерела**. Разом по всьому корпусу: **3 дублі з 1430** (мінімально — джерела здебільшого унікальні).
- Розподіл по типах (усі 1430): rule 521, phrase 237, example 179, mentor_number 219, mistake 108, objection_answer 72, criterion 99 (+3 дублі не входять в жоден рахунок відносно унікальності, вони позначені, не видалені).
- Розподіл по relevance: high 399, medium 1036 (дефолтна класифікація за ключовими словами — потребує ручної звірки на Ф2/Ф3, `relevance_note` це явно каже для кожної картки).
- Портативність пайплайну: ROOT-константи в усіх 5 скриптів `v2/corpus/*.mjs` більше не хардкоджені на Windows-шлях — пайплайн відтворюється однаково і на локальній машині власника, і в хмарній сесії (перевірено: ідентичний результат до/після на 68/82).

**Приклад картки цілком (п.4, усі поля) — типова, contract_valid:true:**
```json
{
 "card_id": "M15-UA_c01_01",
 "source_id": "ОБ-UA15",
 "chunk": "M15-UA_c01",
 "type": "mistake",
 "text_verbatim": "І ти вирішуєш, що вона і в цей раз трохи не візьме, правильно?",
 "locator": "(0:41 - 0:44)",
 "speaker": "Мовець 1",
 "basis": "слова наставника, вказує на помилку менеджера",
 "lesson_hint": "Ілюструє типову помилку новачка — наперед вирішувати за клієнта, скільки він візьме, замість того щоб пропонувати повну суму.",
 "note": null,
 "contract_valid": true,
 "contract_missing_fields": [],
 "is_duplicate": false,
 "duplicate_of": null,
 "duplicate_reason": null,
 "context_before": "[Мовець 2] (0:37 - 0:38)\nВона взяла трохи менше.",
 "context_after": "[Мовець 2] (0:48 - 0:48)\nЗараз.",
 "ambiguous_location": null,
 "relevance": "medium",
 "relevance_note": "не вдалося однозначно класифікувати автоматично — потребує ручної звірки",
 "same_meeting_pair_source": "ОБ-RU15",
 "pair_id": "M15",
 "primary_lang": null,
 "is_primary_source": null
}
```

**Допущення (не питав власника):**
1. Локатор — таймкод з файлу шарду, коли цитата починається одразу після позначки `[Мовець N] (mm:ss - mm:ss)`; коли цитата довга/охоплює кілька реплік — перші 6-10 слів цитати. Це продовжує конвенцію хвиль 1-17 (там теж є обидва варіанти впереміш).
2. Профанну лексику й транскрипційні артефакти (ASR-помилки) в `text_verbatim` не виправляв і не цензурував — це внутрішній робочий корпус, не текст для менеджера (SPEC v2 §4 «мат не воспроизводим» стосується `v2/text/lessons`, не `v2/corpus`).
3. Роботу продовжено в хмарній сесії (та сама назва чату), бо локальна сесія впала на ліміт на хвилі 18 — власник сам запушив гілку `pipeline` на GitHub на прохання. Обидві сесії писали в ту саму гілку послідовно, конфліктів не було.

**Рішення власника (виконано):** дозаповнити 244 картки (не залишати як є). 5 паралельних агентів дозаповнили `lesson_hint` (за наявними `text_verbatim`/`type`/`context_before`/`context_after` картки, без вигадування фактів, у стилі вже заповнених карток) і `speaker` для 8 карток без мітки мовця в шарді (визначено за продовженням репліки наставника з кінця попереднього шарду — `M04-RU_c04`, `M04-UA_c03`). Контракт-перевірка після дозаповнення: **0 з 1435 карток не проходять контракт** (було 244). Дедуп/verify перезапущено — 1430 верифіковано, 3 дублі, 82/82 незмінно.

Далі за рішенням власника: Ф2, потім Ф3, без проміжних звітів у чат (окрім гейтів/помилок/429).

## Ф2. Звід чисел/фактів (33 факти «N з 26» на пулі 99) — В ПРОЦЕСІ

Контекст (знайдено в `v2/handoff/HANDOFF_2026-09-24_corpus-recount-chat.md`): це продовження раніше провального перерахунку — Workflow `ikorka-33-facts-recount` (`wf_6fc9acd5-7b0`) для 26 з 33 фактів повернув 0 даних (session-limit на 258 паралельних викликах агентів), інші 7 — часткові (12-71% покриття). Причина провалу — не логіка, а брак інкрементального збереження прогресу (весь запуск монолітний, без проміжних комітів).

**Блокер (вирішено власником):** R/I/F-класифікація (reactivation/incoming/fblead) 73 нових дзвінків існувала лише в `_from_chat_B/classified_new_calls.json` на локальній машині іншої сесії — гітигнорений файл, у цей чекаут ніколи не потрапляв. За рішенням власника — реконструйовано самостійно: 5 агентів прочитали і класифікували 64 дзвінки (9 вже відомі як incoming за суфіксом файлу) за контентом (reactivation = є посилання на попереднє замовлення; fblead = є посилання на Facebook/Instagram-рекламу/заявку). Результат **56 reactivation + 8 fblead — точний збіг з очікуваними числами RECONCILE.md**, що підтверджує коректність реконструкції. Разом з еталонними (25 R + 1 I, `etalon_22`) — повний пул `v2/corpus/rif_pool.json`: **R=81, I=10, F=8, разом=99** (точний збіг із базами в handoff-таблиці: R=81, R+F=89, R+I+F=99).

**Архітектура переробленого підходу (на відміну від провального Workflow):**
- Замість грід «33 факти × chunk» (258 викликів) — **chunk-based**: кожен агент читає один шматок пулу (~8 файлів) і оцінює ВСІ застосовні з 33 фактів по кожному файлу за один прохід → 13 чанків замість 258 викликів.
- `v2/corpus/facts_plan.json` — 33 факти: id, урок, формулювання критерію, заявлений чисельник, застосовні підгрупи (R/I/F), розмір бази.
- `v2/corpus/rif_pool.json` — 99 файлів з тегом R/I/F.
- `v2/corpus/facts_chunk_plan.json` — розбиття 99 файлів на 13 чанків по 8 (детерміновано, sorted by id).
- Результат кожного чанку — `v2/corpus/facts_eval/fcNN.json`: по кожному файлу — вердикт (так/ні) + дослівна цитата по кожному застосовному факту.
- Коміт після кожної хвилі (як у Ф1) — інкрементальний прогрес переживає session-limit, на відміну від монолітного Workflow.

## Правило звітності (діє з цього моменту)

За вказівкою власника: хвилі продовжуються без проміжних звітів у чат, окрім зупинки на 429 або неочікуваній помилці. Повний звіт — на гейті Ф1 (82/82): Сделано / Допущения / Нужно от тебя.
