# STATE — MVP-тренажёр (ветка `mvp`)

Журнал срезов: после каждого — сборка, автотест, коммит, строка здесь. Push/PR не делаются (требование задачи).

Команды: сборка `node v2/build/build.mjs` · статические проверки `node v2/mvp/tests/static-checks.mjs` · клик-сценарии `node v2/mvp/tests/e2e.mjs [срезы]` · сервер `node v2/coach/server.mjs`.

| Время (UTC) | Фаза | Что сделано | Тест | Статус |
|---|---|---|---|---|
| 2026-09-24 | Ф0 | Ветка `mvp` от `master`; `MVP_PLAN.md` с контентной картой; `v2/mvp/tools/content-lib.mjs` + `validate-content.mjs` (дословность, доли, схема); эталон разметки урока 1; таблица правок долей `stats_redactions.json` (25 правок, только удаление числа); `SHOW_STATS` в сборке; 4 субагента размечают уроки 2–12 | `validate-content.mjs 1` — 0 ошибок | готово |
| 2026-09-24 | Ф1 | `trainer-data.js` (данные дословно из уроков, 602 source_ref), `trainer.js`/`trainer.css`: «Сьогодні» (кольцо, один следующий шаг, «Продовжити з місця», маршрут по дням, дни обучения без штрафа), коды правил урока, «Спроба до пояснення» перед разделом, разбор в 3 строки, пауза в конце раздела (немодальная, одна на раздел, не при загрузке, «Пропустити», возврат фокуса), термины по тапу, «Ви зупинились на…» плашкой в тексте, веха дня + цель на звонки + «Чи вийшло?»; SOS-кнопка «Я на дзвінку» на всех страницах; новые страницы-оболочки (симулятор, тренер, перевірка, керівнику) | `e2e.mjs 1` — 26/26; `static-checks.mjs` — 7/7 | готово |
| 2026-09-24 | Ф2 | Розмітка всіх 12 уроків (`v2/mvp/content/urok_01…12.json`, 0 помилок валідатора): 60 правил, 19 спроб до пояснення, 69 пар, 23 цитати, 21 сцена, 24 терміни. Пари «Типові помилки → Як правильно» з вибором до розкриття; перевірка уроку (самооцінка 1–5 → питання тесту, «сильно/слабко», пари → неправильні повертаються → поріг 80 % з першої спроби → порівняння впевненості з результатом одним рядком → правила до повторення → пригадування відкритих питань); «Повторення» 1/3/7/14 днів, ≤5 карток на день, слабкі першими і з удвічі коротшим інтервалом; особиста колода «Складний клієнт сьогодні» (попередження про персональні дані, маскування цифр) | `e2e.mjs 1 2` — 63/63; `static-checks.mjs` — 7/7 | готово |
| 2026-09-24 | Ф3 | Симулятор дзвінка (`trenazher.html`): 12 сцен, по одній з уроку (перевага — дослівна репліка клієнта, 3 варіанти, без повторів реплік); вибір → розбір у три частини → «Спробувати інший варіант» (обрані позначені) → «Що відпрацювати» (пункт «Практики» + правило) → наступна сцена; список «Що відпрацювати» для сцен з першою помилкою. SOS «Я на дзвінку»: перевірено пошук, вкладки, Esc і повернення фокуса, 375 px. Офлайн: `sw.js` (версія — хеш вмісту, 41 файл), уроки й SOS відкриваються без мережі. Нейтральні формулювання («Знаю / Не знаю» замість «Знав»), favicon | `e2e.mjs 1 2 3` — 99/99; `static-checks.mjs` — 7/7 | готово |
| 2026-09-24 | Ф4 | ІІ-тренер: `v2/coach/server.mjs` (Node без залежностей) роздає сайт і проксує лише `/api/coach/roleplay` і `/api/coach/feedback` → `POST https://api.anthropic.com/v1/messages` (x-api-key з env, anthropic-version 2023-06-01, модель з `COACH_MODEL`, типово claude-sonnet-5, max_tokens 400, thinking вимкнено, тайм-аут 20 с, ≤40 викликів/добу, лічильники без текстів у `v2/coach/data/usage.json` — у .gitignore). Захисти: заземлення ≤3 КБ (правила і фрагменти уроку сцени), ввід менеджера в тегах з інструкцією ігнорувати команди, маскування цифр, вартовий реплік ІІ-клієнта (цифри/відсотки/«безкоштовно» → заміна запереченням з уроку), валідатор розбору (кожна фраза дослівно з уроків і не з переліку помилок, інакше «У курсі немає готової фрази — див. правило N»), офлайн (немає ключа, ліміт, помилка, статичний хостинг) → «Тренер офлайн» і розбір за парами уроків. Сторінка `trener.html`: режим A (18 персон DOC-ZAP + 8 реплік клієнтів з уроків, до 8 реплік, потім розбір), режим B (39 ситуацій). `COACH_MOCK=1` для тестів; `personas.json`/`scenes.json` — порожні заготовки. Живий тест: ключа в середовищі немає — **live не перевірено** | `coach-api.mjs` — 19/19 (+ live пропущено); `e2e.mjs 4` — 32/32; `e2e.mjs 1 2 3` — 99/99 (тепер через сервер тренера); `static-checks.mjs` — 8/8 | готово |
| 2026-09-24 | Ф5 | Перевірка готовності (`perevirka.html`): 24 питання — по два з кожного з 12 уроків упереміш, самооцінка 1–5, поріг 80 %, картка слабких тем (уроки + правила), помилки → «Повторення»; «Сьогодні» після курсу показує результат. Режим керівника (`kerivnyku.html`): зведення по 12 уроках, експорт JSON (без текстів особистої колоди) і CSV (UTF-8 з BOM, `;`), перегляд файлу новачка без зміни свого прогресу, одностраничник «Як супроводжувати новачка» (правила й практика дослівно з уроків). «Сьогодні»: файл прогресу — експорт/імпорт JSON з підтвердженням і очищенням даних. Доступність: axe-core WCAG 2.1 AA — 0 порушень на 9 сторінках × 2 теми (токени: темний `--text-dim` #A09890, темний `--brand` mix 0.36, світлий `--good` #277A4C, новий `--brand-text`; підписи чекбоксів днів; без прозорості в змісті), клавіатура (посилання «до змісту», SOS з клавіатури) | `e2e.mjs 5` — 32/32; регресія `e2e.mjs 1 2 3 4` — 131/131; `static-checks.mjs` — 8/8; `coach-api.mjs` — 19/19 | готово |
| 2026-09-24 | Ф6 | Повний прогін `node v2/mvp/tests/run-all.mjs`: збірка → статичні перевірки `--strict` (усі 12 уроків розмічено, 965 source_ref дослівно, частки приховано, телефонів і ключа немає) → API тренера → клік-сценарії зрізів 1–5 → матриця (27 сторінок × 375/1280 × 2 теми = 108 прогонів; розбір у 3 частини на всіх 12 уроках). `MVP_REPORT.md`; zip `v2/mvp/dist/ikorka-mvp.zip` (сайт + сервер тренера + звіт; перевірено розпакування і запуск) | `results/SUMMARY.json` — 193/193 | готово |
| 2026-09-25 | Н2·П2 | Урок 3: підпис репліки «Я не розумію, що ви пропонуєте?» `хто: Менеджер` → `хто: Клієнтка` (рядок 133; сенс не змінено; діфф — `v2/mvp/CONTENT_FIXES.md`). Вправа «сильно/слабко» бере лише репліки менеджера (цитат 23 → 22) | `static-checks --strict` 9/9; `e2e.mjs 2` | готово |
| 2026-09-25 | Н2·П1 | **Жива перевірка НЕ проведена — немає `ANTHROPIC_API_KEY`** (позначено в MVP_REPORT і COACH_LIVE_TEST.md). Підготовлено `tests/coach-live.mjs` (3 розмови: dz-1, dz-12, s04-1 + розбір; 10 спроб ін'єкції в ролі клієнта і в розборі → COACH_LIVE_TEST.md; код 1, якщо хоч одна пройшла) і `tests/injections.mjs` (10 типів: пряма команда, системний промпт, знижка, ціна/умова, не про продажі, лапки+SYSTEM, переклад, капс/«адмін», «для тестування», закриття тегу). Посилено промпти (не розкривати інструкції, не виходити з ролі, лише українською, репліка-ін'єкція = погано) і вартових (витік промпту, «я — ІІ», сторонні теми, латиниця, гарантії; оцінка «добре» неможлива для репліки з командами або вигаданими цифрами). Мок «зламаної» моделі знайшов пролом (вигадана ціна «499 грн» оцінювалась «добре») — закрито | `coach-api.mjs` 20/20 (10 ін'єкцій × 2 режими заблоковано на моку); `coach-live.mjs` — пропущено (немає ключа) | готово (live — ні) |
| 2026-09-25 | Н2·П3 | Повторний прохід 12 уроків: знайдено дослівно «як правильно» для пар 3-p4 і 7-p3 (пари стали інтерактивними) і готову фразу на «ще є ікра» для сцени s11-2 (урок 4, «Скажи так»; джерело показано в симуляторі). Валідатор і збірка підтримують явні посилання на інший урок (`good_lesson`, `instead_lesson`, `option.lesson`). Не знайдено: 10 сцен без реплік клієнта, 6 пар, фрази для «незручно» (кандидат з уроку 4 суперечить уроку 12 — чекає власника), «скиньте на Viber», «не любить червону ікру» — `v2/mvp/CONTENT_GAPS.md` | `validate-content` 0 помилок; `static-checks --strict` 9/9 (966 source_ref); `e2e.mjs 2 3` 73/73; `e2e.mjs 6` 15/15 | готово |
| 2026-09-25 | Н2·П4 | Рішення власника застосовано: неперевірені твердження скрипта компанії прибрано з SOS і тренажера. SOS-панель — 10 з 18 відповідей DOC-ZAP (ринкова ціна, умови доставки, походження, склад, гарантії, строки); на їхньому місці «Скажи так» уроку 4 або чесна позначка «Перевіреної готової фрази в курсі немає» + правило 4.2/4.3. Сторінка «SOS: скажи так» — 8 з 50 карток (зокрема «пластик за меншу ціну» суперечить уроку 8). Керування — `content/unverified_exclusions.json` (збірка падає, якщо запис не знайдено); перелік з поясненням — `v2/mvp/PHRASES_FOR_REVIEW.md`. Персони ІІ-клієнта (назви заперечень) лишились | `static-checks --strict` 10/10 (новий `content.unverified-claims-hidden`: 31 фрагмент — жодного на сайті); `e2e.mjs 3 7` 43/43 | готово |
| 2026-09-25 | Н2·П5 | `v2/coach/README.md`: лише локальний запуск (127.0.0.1), ключ через змінну оточення (bash/zsh, PowerShell, cmd), `COACH_MODEL`/`COACH_DAILY_LIMIT`/`COACH_MOCK`/`NODE_USE_ENV_PROXY` (перевірено: API з цього середовища досяжне, без ключа — 401), ліміт 40/добу (скидання о 00:00 UTC; що робити при вичерпанні — чекати, підняти ліміт, скинути `usage.json`), 4 способи перевірити офлайн, жива перевірка `coach-live.mjs` | `static-checks` → `docs.coach-readme` ✓ | готово |
| 2026-09-25 | Н2·П6 | `v2/mvp/DESIGN_SYSTEM_CHANGES.md`: 4 токени (темні `--text-dim`, `--brand`; світлий `--good`; новий `--brand-text`) — було → стало → контраст до/після → де помітно; 5 точкових правок компонентів; що звести з перевіркою дизайну уроків 2–12 (відхилення темного `--brand` від мокапу — погодити) | `static-checks` → `docs.design-system-changes` (звіряє опис з поточними значеннями `tokens.mjs`) ✓; усього 12/12 | готово |
| 2026-09-25 | Н2·П7 | Повний прогін `run-all.mjs`: 219 пройдено / 0 не пройдено (193 ночі 1 + 26 нових: статичні +3, API тренера +1, клік-сценарії +22); жива перевірка — «ПРОПУЩЕНО: немає ANTHROPIC_API_KEY» (окремо, не рахується як пройдена). MVP_REPORT — розділ «Ніч 2». Перезібрано `ikorka-mvp.zip` (+ README тренера) і новий бандл `ikorka-mvp-branch.bundle` | `results/SUMMARY.json` 219/219 | готово |
| 2026-09-25 | Н3·Зр1 | Гілка `public-deploy` від `mvp`. Auth + ролі + прогрес у Supabase: `supabase/migrations/0001_init.sql` (profiles/progress, RLS, тригер автопрофілю, захист від самопідвищення ролі); `v2/build/assets/account.js` (вхід/реєстрація/вихід, синхронізація прогресу localStorage↔хмара за монотонним `rev`, реюз `sanitizeState`/`replaceState` з trainer.js), сторінка `account.html`, `supabase-config.js` генерується з env `SUPABASE_URL`/`SUPABASE_ANON_KEY` при збірці — без них акаунти вимкнені повністю, сайт як і раніше офлайн (перевірено регресією). Знайдено й виправлено 2 реальні баги в SQL через живий тест: рекурсія RLS-політики (винесено в `is_mentor()`) і хибна перевірка service_role. `SUPABASE_SETUP.md` — покрокова інструкція + призначення ролі «керівник». Мережевий доступ до cdn.jsdelivr.net заборонений політикою середовища (403) — supabase-js для тестів встановлено локально через npm і підмінюється в Playwright-роуті; продакшн-код не змінювався | `rls-test.mjs` — 10/10 (реальний Postgres 16, 7 сценаріїв RLS); `account-e2e.mjs` — 23/23 (наближений мок GoTrue+PostgREST); повний прогін `run-all.mjs` — 252/252 (+ жива перевірка тренера пропущена, як і раніше) | готово — чекає перевірки власника |
| 2026-09-25 | Н3·Зр1-правки | 5 правок власника до зрізу 1 перед зрізом 2. (1) **Лише запрошення**: публічний signup прибрано з `account.html`, реєстрація — тільки через Supabase Dashboard → Invite user → лист із посиланням → форма «встановити пароль» (той самий механізм PASSWORD_RECOVERY, що й відновлення; `account.js` тепер визначає `invite`/`recovery` напряму з хеша адреси, бо supabase-js емітить подію `PASSWORD_RECOVERY` лише для `type=recovery`, не для `type=invite` — з подією саме по собі запрошення показувало б акаунт без форми пароля). (2) **Злиття прогресу замість «останній виграє»**: `mergeState()`/union-max по кожному полю (уроки, повторення, симулятор, колода, дні, позиції) — `account.js`; `pushProgress()`/`reconcile()` тепер мають захист від паралельних викликів (`*InFlight`), інакше таймер і ручний виклик могли піти в мережу одночасно. Під час налагодження тесту «два пристрої» знайдено й виправлено **реальний баг**: порівняння стану через `JSON.stringify` було чутливим до порядку ключів (`sanitizeState()` завжди перебудовує в канонічному порядку, `mergeState()` успадковує порядок від `base`) і до ключів зі значенням `undefined` (`test.conf`, коли жоден бік його не має, — `JSON.stringify` мовчки прибирає такий ключ при збереженні, а щойно порахований `merged` ще ні) — обидва разом інколи змушували `reconcile()` перезавантажувати сторінку в нескінченному циклі навіть без жодної реальної розбіжності; замінено на рекурсивне `deepEqual`, що ігнорує порядок ключів і трактує `undefined`-значення як відсутній ключ. (3) **Вендоринг замість CDN**: `v2/build/assets/vendor/supabase-js.js` (npm `@supabase/supabase-js@2.117.2`, UMD, README з процедурою оновлення) — і продакшн, і тести тепер вантажать один і той самий файл, `cdn.jsdelivr.net` більше не потрібен. (4) `SUPABASE_SETUP.md`: крок про власний SMTP (ліміт вбудованої пошти Supabase — кілька листів/годину). (5) `PUBLIC_DEPLOY_PLAN.md`: зафіксовано вимогу зрізу 5 — контент курсу лише після входу, гейтинг через Netlify Edge Function з перевіркою сесії Supabase (статичні сторінки на CDN самі по собі публічні, клієнтського JS для розмежування не досить) | `account-e2e.mjs` — 40/40 (15 прогонів поспіль без відмов — раніше траплялась гонитва reload/ERR_ABORTED через описаний вище баг); повний прогін `run-all.mjs` — 269/269 (+ жива перевірка тренера пропущена) | готово — жива перевірка проти реального Supabase-проєкту власник проводить сам |
| 2026-09-25 | Н3·Зр2 | Кабінет наставника + 4 вимоги власника до зрізу 2. `supabase/migrations/0002_reset_and_attempts.sql`: `progress.generation` (бампається лише скиданням), `reset_progress(user_id, lesson_n)` — SECURITY DEFINER, сам перевіряє `is_mentor()`, весь або по уроку (`state.lessons[N]`, картки повторення/симулятор не чіпає — свідомо менший обсяг); таблиця `attempts` — append-only журнал спроб тесту/тренажера (урок, дата, бал/пройдено, коди правил-помилок, без текстів), RLS: новачок пише/читає своє, наставник читає все, update/delete немає в жодної ролі. **(1) generation**: `account.js` `reconcile()` звіряє локальне покоління з хмарним — якщо хмарне більше, локальний кеш ЗАМІНЮЄТЬСЯ хмарою повністю (без union), інакше старий кеш «воскресив» би скинутий урок. **(2) attempts**: хуки в `trainer.js` (`finish()` тесту й `choose()` тренажера) шлють `window.ACCOUNT.recordAttempt()` за наявності акаунта; знайдено й виправлено реальний баг під час першого ж живого прогону — виклик `.catch()` напряму на білдері PostgREST падав з «.catch is not a function» (білдер — thenable, має лише `.then()`; виправлено на `.then(ok, err)`). **(3) reload лише при вході/відкритті**: `reconcile()` розрізняє початковий виклик (після входу/завантаження сторінки — застосовує злиття й може перезавантажити) і фоновий (таймер/зміна вкладки — лише пушить злитий стан у хмару, DOM і локальний стан не чіпає, щоб не губити введення в тренажері/тесті посеред відповіді); застосування — природно на наступному переході. **(4) сканер приватності**: замість виключення всієї `vendor/` — виключення конкретного файлу `assets/vendor/supabase-js.js` лише за збігом SHA-256 з пришпиленим поруч `.sha256` (перевірено обома напрямками: збіг — пропускає, розбіжність — сканує як звичайний файл). Кабінет наставника: `kerivnyku.html` → блок «Новачки (хмара)» (лише під роллю «керівник»; `MVP.summaryHtml` довантажено в `window.MVP` для перевикористання таблиці прогресу) — список новачків, прогрес по уроках, остання активність, історія спроб, кнопки скидання (урок/весь прогрес, з `window.confirm`). Знайдено й виправлено ще один реальний баг: `data-uid` на самих кнопках скидання підмінював `btn.closest('[data-uid]')` — знаходив саму кнопку замість картки новачка (`closest()` перевіряє спершу сам елемент) — прибрано, `data-uid` лишився лише на картці | `rls-test.mjs` — 18/18 (15 сценаріїв SQL, включно з 8 новими для reset_progress/attempts); `account-e2e.mjs` — 62/62 (6 прогонів поспіль без відмов; 3 нові сценарії: скидання й «старий браузер», reload-таймінг посеред тесту, спроби+кабінет наставника з реальним кліком кнопки скидання); `static-checks --strict` — 12/12; повний прогін `run-all.mjs` — 299/299 (+ жива перевірка тренера пропущена) | готово — жива перевірка проти реального Supabase-проєкту власник проводить сам |
